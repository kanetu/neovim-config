// Visualize -> Copy -> Paste
v -> y -> p

// Open NERDTree
Ctrl + k

// Search file 
Ctrl + p

// Switch NERDTree to Tab and verse
Ctrl + WW

// Switch between tabs (Arrow)
Shift + -> | Shift + <-

// Create new tab
:tabnew <filename>
